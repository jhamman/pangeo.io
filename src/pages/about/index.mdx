import { Layout } from '@/components'
import {Container, Box} from '@chakra-ui/react'


# About

About Pangeo...

export default ({ children }) => <Layout><Box><Container maxW='container.xl' py={90}>{children}</Container></Box></Layout>
